# ESCAPE=`

# it is convention to write parser directives in lowercase
# see: https://docs.docker.com/engine/reference/builder/#parser-directives
FROM alpine:3.13.5

RUN echo `
    "Hello Build!"

CMD ["echo", "Hello World!"]